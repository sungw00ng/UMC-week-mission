// src/main/java/week4/domain/Enums.java

package week7.domain;

public class Enums {

    // User 엔티티에서 사용
    public enum Gender { MALE, FEMALE, NONE }
    public enum SocialType { KAKAO, NAVER, APPLE, GOOGLE }

    // Term 엔티티에서 사용
    public enum TermName { AGE, SERVICE, PRIVACY, LOCATION, MARKETING }
}